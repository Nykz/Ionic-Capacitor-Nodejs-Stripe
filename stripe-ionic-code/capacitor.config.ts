import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.stripePayTestApp',
  appName: 'stripe-payment-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
